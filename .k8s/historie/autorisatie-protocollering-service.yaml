apiVersion: v1
kind: Service
metadata:
  name: historie-autorisatie-protocollering
  labels:
    app: historie-autorisatie-protocollering
spec:
  selector:
    name: historie-autorisatie-protocollering
  # type: ClusterIP
  type: LoadBalancer
  ports:
    - name: "8080"
      port: 8080
      targetPort: 8080
