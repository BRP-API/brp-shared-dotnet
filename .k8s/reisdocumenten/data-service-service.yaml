apiVersion: v1
kind: Service
metadata:
  name: reisdocumenten-data-service
  labels:
    app: reisdocumenten-data-service
spec:
  selector:
    name: reisdocumenten-data-service
  # type: ClusterIP
  type: LoadBalancer
  ports:
    - name: http
      port: 8000
      targetPort: 80
