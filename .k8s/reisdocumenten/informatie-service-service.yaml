apiVersion: v1
kind: Service
metadata:
  name: reisdocumenten-informatie-service
  labels:
    app: reisdocumenten-informatie-service
spec:
  selector:
    name: reisdocumenten-informatie-service
  # type: ClusterIP
  type: LoadBalancer
  ports:
    - name: "5000"
      port: 5000
      targetPort: 5000
