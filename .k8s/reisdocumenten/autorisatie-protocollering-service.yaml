apiVersion: v1
kind: Service
metadata:
  name: reisdocumenten-autorisatie-protocollering
  labels:
    app: reisdocumenten-autorisatie-protocollering
spec:
  selector:
    name: reisdocumenten-autorisatie-protocollering
  # type: ClusterIP
  type: LoadBalancer
  ports:
    - name: "8080"
      port: 8080
      targetPort: 8080
