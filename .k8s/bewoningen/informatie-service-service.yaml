apiVersion: v1
kind: Service
metadata:
  name: bewoningen-informatie-service
  labels:
    app: bewoningen-informatie-service
spec:
  selector:
    name: bewoningen-informatie-service
  # type: ClusterIP
  type: LoadBalancer
  ports:
    - name: "5000"
      port: 5000
      targetPort: 5000
