apiVersion: v1
kind: Service
metadata:
  name: bewoningen-autorisatie-protocollering
  labels:
    app: bewoningen-autorisatie-protocollering
spec:
  selector:
    name: bewoningen-autorisatie-protocollering
  # type: ClusterIP
  type: LoadBalancer
  ports:
    - name: "8080"
      port: 8080
      targetPort: 8080
